// // 1. Replace the function expressions with arrow functions in the following code:

// function ask(question, yes, no) {  
//     if (confirm(question)) yes();  
//     else no();  
// }  
// ask(  
//     "Do you agree?",  
//     function() { alert("You agreed."); },  
//     function() { alert("You canceled the execution."); }  
// ); 





// // 2. Identify the output of the following code:

// const materials = ['Hydrogen', 'Helium', 'Lithium', 'Beryllium'];  
// console.log(materials.map((material) => material.length));




// // 3. Write an arrow function called square that takes a number as an argument and 
// returns its square.





// // 4. Given an array of numbers, use an arrow function to create a new array 
// containing the squares of those numbers. For example, if the input is [1, 2, 3], 
// the output should be [1, 4, 9].






// // 5. Use an arrow function to reverse a given string. For example, if the input is 
// "hello", the output should be "olleh".








// // 6. Create a chainable object using arrow functions that allows setting a value and 
// then adding or multiplying it by a number. For example:

// const result = chainable(5).add(3).multiply(2);  
// // result should be 16






